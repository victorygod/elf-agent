1. 提示词注入应该有多种点位，比如可以就在system后面（和system提示词合并）加一个当前场景的section；再比如在最近一个user消息前注入、在最近一个user消息后注入（区分是合并消息还是单独一条，是否有类似is_meta等属性，其实就相当于现在prefix和suffix之类的，只不过我觉得这些应该放在hook里做而不是特殊的massageManager做，我们可以设计一个比较清晰的hook方案，一方面在agent里留好hook，另一方面场景插件、config配置都可以向这些点位注入，elf-001/elf-003的prefix和suffix配置就不用单独写一个message manager了）这里我觉得注入点位有这么几个讲究：a.是否持久化到context.json里（即是否是临时性的）;b. 是否是is_meta的，即是否真的会发给llm；c。是否是独立消息还是拼合在前/后又或者是文本替换等操作；d.具体位置。
2. _enterRunLevel放在哪里比较合适？harness？插件？
3. agent的_mw、_dispatchGate、_emit、_runInjection是否真的需要？可否直接内联？updateModel、updateMessageManagerConfig、abort等是否应该放到harness里？（是否真的是无调用方的死代码？不包括测试哈）重新评估一下harness应该有什么作用吧
4. 群聊里怎么不按照agent的配置进行压缩呢？看起来到了应该压缩的地方没有触发压缩，要不在压缩判断的地方打日志吧，带上当前token数以及为什么没进入压缩等信息。
5. 现在context和history分散在chat和room目录下，且解散群聊之后不知道删的哪个目录。我们这样吧：context、tool-results、checkpoints、sync_cursor都放在agent/data目录下，history.jsonl、room.json这种房间的专属配置可以放在rooms里，实际上这些都属于profiles，也就是说agents目录和rooms目录以及logs目录应该都放在profiles目录（需要新建）下。
6. 关于skills，我们不要再支持多目录了，就统一放在一个目录里好了，就是现在哪个~/.elf那里。
7. 群聊/私聊里llm请求失败重试在前端设个气泡吧，重试了几次都写明白。
8. 私聊里的history.jsonl的格式怎么改了？现在前端刷一下全都乱了。你翻一下git log，很早以前的agents/elf-002/data/history.jsonl的格式是什么样的。我大概给你讲一下，是具有高度时序性的，和流式聊天的前端渲染出来的顺序保持高度统一的，每条消息都是一行，然后需要更新的消息不重复存多行，比如多次重试的compact就只存一条，然后field里填写重试第几次。每个assistant消息都需要存独立的一条，即使content为null或“”也得存，tool call和tool result存在一起（你看看以前，应该是的）实在找不到的话可以看下docs/example/history.jsonl，是一个例子；然后有人分析说vite.config.js的proxy只代理了/agents和/api，漏了/rooms，这个有影响么？然后分析下来发现，private_room_stream.js有问题把整turn给累加落盘了，实际上不应该这样，你看下它怎么来的，应该是迁移的时候逻辑不对。实际上群聊的history.jsonl不落tool，私聊的落tool，这二者格式本身就是不一致的，不应该统一。