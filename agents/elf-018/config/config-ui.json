{
  "tabs": [
    {
      "key": "game-state",
      "label": "游戏状态",
      "type": "game-state",
      "fields": []
    },
    {
      "key": "prompt",
      "label": "提示词配置",
      "fields": [
        { "key": "systemPrompt", "type": "textarea", "label": "总纲 system_prompt", "hint": "DM 人设总纲 + 正典规则 + 基础世界观（所有 loop 的 system 基底）" },
        { "key": "loop_outline_prompt", "type": "textarea", "label": "outline 大纲任务", "hint": "outline loop 任务：写大纲 + 自查维护 lore/面板/state.md（useAppend 注入末尾）" },
        { "key": "loop_render_prompt", "type": "textarea", "label": "render 渲染任务", "hint": "render loop 渲染指令（system-reminder 注入）" },
        { "key": "renderExamples", "type": "textarea", "label": "render 描写样例", "hint": "render 风格样例正文（战前轮/战斗/暴击/探索/对话/断章选项），写回 render_examples.md" },
        { "key": "compactPrompt", "type": "textarea", "label": "历史压缩指令", "hint": "_doHistoryCompact 压缩历史大纲的指令前缀" },
        { "key": "compactSystemPrompt", "type": "textarea", "label": "压缩系统提示词（可空）", "hint": "压缩请求的 system，留空则压缩请求不带 system" }
      ]
    },
    {
      "key": "agent",
      "label": "Agent 配置",
      "fields": [
        { "key": "avatar", "type": "avatar" },
        { "key": "name", "type": "text", "label": "Agent 名称", "hint": "前端显示名" },
        { "key": "tools", "type": "readonly-tags", "label": "已启用工具", "hint": "config.json tools 字段配置后重启生效" },
        { "key": "memoryTokenLimit", "type": "number", "label": "记忆压缩 Token 上限", "hint": "MM 压缩阈值（当前 DNDMM no-op，预留）" },
        { "key": "maxIterations", "type": "number", "label": "最大迭代轮数", "hint": "单 loop 内工具循环上限" }
      ]
    },
    {
      "key": "model",
      "label": "模型配置",
      "fields": [
        { "key": "base_url", "type": "text", "label": "API Base URL", "hint": "LLM API 端点" },
        { "key": "auth_token", "type": "text", "label": "Auth Token", "hint": "LLM API 认证密钥" },
        { "key": "model", "type": "text", "label": "模型名称", "hint": "如 gpt-4o、GLM-5.1、deepseek-chat" }
      ]
    }
  ]
}